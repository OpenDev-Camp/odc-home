import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  inner: {
    paddingTop: '30px',
    marginBottom: '1vh',
  },
  outer: {
    display: 'block',
  },
});
