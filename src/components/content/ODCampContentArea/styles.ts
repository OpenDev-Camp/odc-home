import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  outer: {
    display: 'block',
  },
  inner: {
    paddingTop: '30px',
    marginBottom: '1vh',
  },
});
